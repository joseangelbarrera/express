form(action='/cart', method='POST')
  input(type='text', name='item')
  input(type='number', name='quantity')
  button Add to Cart!

ul
  each itemCart in cart
    li #{itemCart.item} → #{itemCart.quantity}
